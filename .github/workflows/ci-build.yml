name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  productNamespacePrefix: "Splat"

jobs:
  call-workflow-setup-and-build:
    uses: reactiveui/splat/.github/workflows/workflow-common-setup-and-build.yml@93a3b700ef406751d98061b41fa970a372d84165
    with:
      configuration: Release
      useVisualStudioPreview: false
      useMauiCheckDotNetTool: false
      runTests: true
      nugetMode: 1
