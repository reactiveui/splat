name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  productNamespacePrefix: "Splat"

jobs:
  build:
    uses: reactiveui/actions-common/.github/workflows/workflow-common-setup-and-build.yml@aafa6cacb7563a588ca7ab02f4dd34c077343e96
    with:
      configuration: Release
      productNamespacePrefix: "Splat"
      useVisualStudioPreview: false
      useMauiCheckDotNetTool: false
      runTests: true
      nugetMode: 1
