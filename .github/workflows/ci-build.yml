name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  productNamespacePrefix: "Splat"

jobs:
  call-workflow-setup-and-build:
    uses: reactiveui/splat/.github/workflows/workflow-common-setup-and-build.yml@2ddb0bbb8c15f5bd9e8ed1c230a16c67a07f490a
    with:
      configuration: Release
      useVisualStudioPreview: false
      useMauiCheckDotNetTool: false
      runTests: true
      nugetMode: 1
