name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  productNamespacePrefix: "Splat"

jobs:
  build:
    uses: reactiveui/splat/.github/workflows/workflow-common-setup-and-build.yml@31f963cd1707f1a420a18d85cc9fcc21948ddd76
    with:
      configuration: Release
      productNamespacePrefix: "Splat"
      useVisualStudioPreview: false
      useMauiCheckDotNetTool: false
      runTests: true
      nugetMode: 1
