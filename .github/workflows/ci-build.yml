name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  productNamespacePrefix: "Splat"

jobs:
  call-workflow-setup-and-build:
    uses: reactiveui/splat/.github/workflows/workflow-common-setup-and-build.yml@7a918f28b9d8195f947d558784e7fa44bda67d7e
    with:
      configuration: Release
      useVisualStudioPreview: false
      useMauiCheckDotNetTool: false
      runTests: true
      createPackages: true
